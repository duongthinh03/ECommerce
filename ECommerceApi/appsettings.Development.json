{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
  // ConnectionStrings:DefaultConnection -> lưu trong user-secrets (dotnet user-secrets list)
  // KHÔNG để password trong file này (tránh commit secret vào git).
}
